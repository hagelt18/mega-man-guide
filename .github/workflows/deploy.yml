name: Deploy to github pages
on:
  push:
    branches: main
  pull_request:
    types: [labeled]
jobs:
  testjob1:
    runs-on: ubuntu-latest
    steps:
      - name: grr
        run: echo "grrrr"
  testjob2:
    uses: "./.github/workflows/build.yml"
  deploy:
    needs: [testjob1, testjob2]
    runs-on: ubuntu-latest
    steps:
      - name: 🏗 Export web files
        run: expo export:web

      - name: 🚀 Deploy to Github Pages
        run: gh-pages -d web-build
