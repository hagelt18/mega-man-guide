name: Deploy to github pages
on:
  push:
    branches: main
  pull_request:
    types: [labeled]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: 🏗 Export web files
        run: expo export:web

      - name: 🚀 Deploy to Github Pages
        run: gh-pages -d web-build
